{
  "name": "tribal-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Utility scripts for the Tribal Wars PHP remake; no Node build is required for the static assets.",
  "license": "MIT",
  "scripts": {
    "build": "node -e \"console.log('Nothing to build; PHP/JS assets are served as-is.')\""
  },
  "engines": {
    "node": ">=18"
  }
}
